from __future__ import annotations
import sys
from importlib.machinery import ModuleSpec, PathFinder
from importlib.machinery import all_suffixes as module_suffixes
from importlib.util import spec_from_file_location
from itertools import chain
from pathlib import Path

MAPPING: dict[str, str] = {'aider': '/home/samko/Documents/GitHub/planning_test/myenv/aider/aider'}
NAMESPACES: dict[str, list[str]] = {'aider.website': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website'], 'aider.queries': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/queries'], 'aider.website._includes': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/_includes'], 'aider.website.share': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/share'], 'aider.website._layouts': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/_layouts'], 'aider.website.assets': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/assets'], 'aider.website.docs': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/docs'], 'aider.website._data': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/_data'], 'aider.website._sass': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/_sass'], 'aider.website.examples': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/examples'], 'aider.website.blog': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/blog'], 'aider.website._posts': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/_posts'], 'aider.website.assets.icons': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/assets/icons'], 'aider.website.docs.troubleshooting': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/docs/troubleshooting'], 'aider.website.docs.more': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/docs/more'], 'aider.website.docs.usage': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/docs/usage'], 'aider.website.docs.config': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/docs/config'], 'aider.website.docs.install': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/docs/install'], 'aider.website.docs.llms': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/docs/llms'], 'aider.website.docs.leaderboards': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/docs/leaderboards'], 'aider.website._sass.custom': ['/home/samko/Documents/GitHub/planning_test/myenv/aider/aider/website/_sass/custom']}
PATH_PLACEHOLDER = '__editable__.aider_chat-0.58.1.dev8+g0aaa37f5.finder' + ".__path_hook__"


class _EditableFinder:  # MetaPathFinder
    @classmethod
    def find_spec(cls, fullname: str, path=None, target=None) -> ModuleSpec | None:  # type: ignore
        # Top-level packages and modules (we know these exist in the FS)
        if fullname in MAPPING:
            pkg_path = MAPPING[fullname]
            return cls._find_spec(fullname, Path(pkg_path))

        # Handle immediate children modules (required for namespaces to work)
        # To avoid problems with case sensitivity in the file system we delegate
        # to the importlib.machinery implementation.
        parent, _, child = fullname.rpartition(".")
        if parent and parent in MAPPING:
            return PathFinder.find_spec(fullname, path=[MAPPING[parent]])

        # Other levels of nesting should be handled automatically by importlib
        # using the parent path.
        return None

    @classmethod
    def _find_spec(cls, fullname: str, candidate_path: Path) -> ModuleSpec | None:
        init = candidate_path / "__init__.py"
        candidates = (candidate_path.with_suffix(x) for x in module_suffixes())
        for candidate in chain([init], candidates):
            if candidate.exists():
                return spec_from_file_location(fullname, candidate)
        return None


class _EditableNamespaceFinder:  # PathEntryFinder
    @classmethod
    def _path_hook(cls, path) -> type[_EditableNamespaceFinder]:
        if path == PATH_PLACEHOLDER:
            return cls
        raise ImportError

    @classmethod
    def _paths(cls, fullname: str) -> list[str]:
        paths = NAMESPACES[fullname]
        if not paths and fullname in MAPPING:
            paths = [MAPPING[fullname]]
        # Always add placeholder, for 2 reasons:
        # 1. __path__ cannot be empty for the spec to be considered namespace.
        # 2. In the case of nested namespaces, we need to force
        #    import machinery to query _EditableNamespaceFinder again.
        return [*paths, PATH_PLACEHOLDER]

    @classmethod
    def find_spec(cls, fullname: str, target=None) -> ModuleSpec | None:  # type: ignore
        if fullname in NAMESPACES:
            spec = ModuleSpec(fullname, None, is_package=True)
            spec.submodule_search_locations = cls._paths(fullname)
            return spec
        return None

    @classmethod
    def find_module(cls, _fullname) -> None:
        return None


def install():
    if not any(finder == _EditableFinder for finder in sys.meta_path):
        sys.meta_path.append(_EditableFinder)

    if not NAMESPACES:
        return

    if not any(hook == _EditableNamespaceFinder._path_hook for hook in sys.path_hooks):
        # PathEntryFinder is needed to create NamespaceSpec without private APIS
        sys.path_hooks.append(_EditableNamespaceFinder._path_hook)
    if PATH_PLACEHOLDER not in sys.path:
        sys.path.append(PATH_PLACEHOLDER)  # Used just to trigger the path hook
